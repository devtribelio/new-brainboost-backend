node_modules/
dist/
coverage/
.env
.env.*
!.env.example
*.log

# secrets — credential files (FCM service account, etc). Folder tracked via README only.
secrets/*
secrets/README.md
.DS_Store
.idea/
.vscode/
tmp/
uploads/

# ruv-swarm / claude-flow artifacts
.swarm/
.claude-flow/
.claude/
.mcp.json
ruvector.db

# media migration runtime state (Model C — see docs/media-model-c-migration.md)
# media-guid-map.json IS committed — the old→new guid map is needed to run the
# `rewrite` phase on other environments (dev/staging) with the same Bunny library.
scripts/media-guids.json
scripts/media-copy-failures.json

# TS incremental build info
*.tsbuildinfo
scripts/member-redirect.json

# Pre-apply snapshots written by scripts/backfill-rc-currency.ts. Production row
# data (payment ids + amounts) and only useful on the machine that ran the repair —
# keep them locally as the rollback reference, out of the repo.
scripts/backfill-rc-currency.before-*.json
