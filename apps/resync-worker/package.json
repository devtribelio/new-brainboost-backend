{
  "name": "@bb/resync-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Transition-period tool: incremental resync of already-migrated data from legacy MariaDB into Postgres. Throwaway after cutover. Spec: docs/legacy-resync-plan.md.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bb/common": "workspace:*",
    "@prisma/client": "^5.22.0",
    "dotenv": "^16.4.5",
    "mysql2": "^3.22.3"
  }
}
